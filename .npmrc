# Garantir que devDependencies sejam instaladas mesmo com NODE_ENV=production.
# Necessário para Hostinger Node.js Apps, onde o build do Next.js depende
# de tailwindcss, postcss, autoprefixer, typescript e @types/* que ficam
# em devDependencies.
production=false
legacy-peer-deps=true
